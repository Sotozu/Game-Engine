# Game-Engine
Created a data driven, cross platform game engine which parses JSON as a configuration language.
Implemented various software engineering patterns including the command, observer, chain of responsibility, composite, strategy, and singleton patterns.
Implemented a custom container capable of dynamic typing at runtime through the use of a discriminated union.
