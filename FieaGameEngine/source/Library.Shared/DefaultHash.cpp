#include "pch.h"
#include "DefaultHash.h"

namespace FieaGameEngine
{
	//inline size_t AdditiveHash(const uint8_t* data, size_t size)
	//{
	//	size_t hash = 31; //seed the hash with a prime number.

	//	for (size_t i = 0; i < size; ++i) //additive hash
	//	{
	//		hash += data[i];
	//	}

	//	return hash;
	//}
}